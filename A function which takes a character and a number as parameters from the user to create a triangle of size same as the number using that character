def pypart(n):
    
    myList = []

    c=input("Enter the character: ")
    for i in range(1,n+1):

        myList.append(c*i)

    print("\n".join(myList))

n=int(input("Enter the size of the triangle: "))

pypart(n)
